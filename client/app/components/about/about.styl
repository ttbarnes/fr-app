@import '../../common'

.about

  .main-content
    font-size 1.1em
    max-width $section-max-width
    margin 0 auto

    p
      span.quote,
      span.highlight
        color $color-highlight-primary

      span.quote
        font-style italic

    blockquote
      &.first
        margin-bottom $gutter-primary-lg
